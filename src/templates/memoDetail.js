import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  display: table-cell;
  position: relative;
  width: calc(100% - 470px);
  height: 100%;
`;

const MemoDetail = () => {
  return <Wrapper>MemoDetail</Wrapper>;
};

export default MemoDetail;
